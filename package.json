{
  "name": "IPSSI_Projet_web",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm run --stream --parallel dev",
    "build": "pnpm run --stream --parallel build",
    "start": "pnpm run --stream --parallel start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "biome check --write --config-path biome.json",
    "format": "prettier . --write",
    "lint": "biome lint --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.13.0",
    "prettier": "3.4.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9.5"
  },
  "packageManager": "pnpm@9.5.0",
  "dependencies": {
    "tsx": "^4.19.2"
  }
}
